# 本机默认语音与试听

0.7.5 新增 Windows 本机系统语音，目的是在没有火山引擎账号和 Key 的情况下，先完整测试“分镜 → 配音 → 合成视频”流程。

## 使用方法

1. 打开 **设置 → TTS 配音**。
2. 选择 **本机系统语音**。
3. 在“本机音色”中选择 Windows 已安装的声音，或保留“系统默认音色”。
4. 点击 **生成试听**，页面会直接出现音频播放器。
5. 点击右上角 **保存设置**。
6. 返回“开始创作”，配音员区域会显示本机系统语音。

本机语音：

- 免费；
- 不需要 API Key；
- 不需要联网；
- 生成 WAV 文件；
- 仅支持 Windows。

## 没有中文音色怎么办

软件只能读取 Windows 已安装的 SAPI 音色。如果列表为空，或只有英文声音，请在 Windows 的语言/语音设置中安装中文语音包，然后重新打开软件。

常见路径：

`Windows 设置 → 时间和语言 → 语言和区域 → 中文（简体）→ 语言选项 → 语音`

安装后重新启动 Storybound，再进入 TTS 设置刷新。

## 火山引擎

需要更自然的正式配音时，再切换到“火山引擎”，填写 App ID 和 Access Token。两种引擎可以随时切换，已经生成的旧音频不会自动删除。

## 0.7.6：官方公开样音

火山引擎音色卡片现在支持两种试听：

- **官方样音**：直接播放火山引擎官网公开试听文件，不需要 App ID、Access Token，也不消耗合成额度。
- **生成试听**：使用当前填写的 App ID、Access Token、音色 ID 和参数实时合成，用来验证正式接口。

本版本已加入：

- 东方浩然 2.0：`zh_male_dongfanghaoran_uranus_bigtts`
- 擎苍：`zh_male_qingcang_mars_bigtts`

官方样音地址只用于在线试听，不会被打包进安装包。若官网更换或下线地址，软件会播放失败，但不会影响正式 TTS 合成功能。
