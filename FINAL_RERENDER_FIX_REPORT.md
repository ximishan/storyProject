# Storybound 0.8.5 最终视频重新合成修复说明

## 本次解决的问题

1. `final.mp4` 只包含画面和配音，字幕、主标题、副标题、免责声明仅存在于剪映草稿中。
2. 默认 `zoompan` 逐帧缩放在竖屏画面中可能出现像素取整抖动。
3. 修改动画后，旧版仍可能复用 `render/*.mp4` 缓存，导致动画实际上没有变化。
4. 已经生成全部图片和配音后，缺少一个明确的“只重新合成、不重新调用接口”的入口。

## 0.8.5 新功能

### 使用现有素材重新合成

任务工作台点击“使用现有素材重新合成”后，可选择：

- 无动画
- 左拉镜
- 右拉镜
- 交替拉镜
- 其他原有动画
- 运镜强度
- 是否强制使用静态图片
- 是否烧录旁白字幕
- 是否烧录主标题
- 是否烧录副标题
- 是否烧录安全提示/免责声明

重新合成不会重新请求文案、图片或配音接口。

### 新输出文件

重新合成生成：

```text
final-rerender.mp4
```

原来的 `final.mp4` 会保留，避免覆盖旧成品。

### 抖动修复

“左拉镜”和“右拉镜”改为：

- 图片先固定放大到稳定尺寸；
- 只移动裁剪窗口；
- 坐标固定为偶数整数像素；
- 输入与输出固定 30fps；
- 不再使用逐帧变化的缩放尺寸。

默认模板动画由“缩放”改为“左拉镜”。

### 文字图层修复

最终 MP4 现在使用 ASS 图层统一烧录：

- 主标题
- 副标题
- 分镜旁白字幕
- 安全提示/免责声明

文字颜色、透明度、字号、描边、背景和位置读取草稿模板配置。

### 缓存处理

点击重新合成时会删除旧 `render` 缓存，确保新动画真正生效。

## 使用方法

1. 覆盖到当前源码目录。
2. 执行：

```powershell
npm run dev
```

3. 打开已完成任务。
4. 点击“使用现有素材重新合成”。
5. 推荐选择“左拉镜”或“交替拉镜”。
6. 保持字幕、标题和安全提示勾选。
7. 点击“开始重新合成”。
8. 完成后点击“播放视频”，或打开产物目录查看 `final-rerender.mp4`。

## 验证

- Electron 主进程相关 `.cjs` 文件均通过 `node --check`。
- `App.tsx` 通过 TypeScript `transpileModule` 语法检查。
- 使用本地 FFmpeg 实际生成测试视频成功。
- 已验证 ASS 中文标题、副标题、字幕和免责声明可正常烧录。
- 已验证带背景音乐和不带背景音乐两种合成流程。
