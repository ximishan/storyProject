# Storybound 0.8.3：重做画面无反馈与轮询卡住修复

## 问题原因

0.8.2 在收到 `response.poll_url` 后，会同步等待远程图片任务完成，默认最多约 360 秒。
旧版只有在最终失败或超时的时候才写入 `image-debug/<镜头号>-poll.json`，轮询过程中界面也没有显示查询次数和已等待时间，因此点击“重做画面”后看起来像没有反应。

另外，旧版轮询 GET 请求没有单次请求超时。如果中转站连接不返回数据，而不是立即返回 502，单次请求可能长期挂起，导致总超时逻辑无法及时执行。

## 0.8.3 修改

- 点击“重做画面”后立即显示“已开始，请不要重复点击”。
- 按钮切换为旋转图标与“生成中…”。
- 每次轮询都会把进度发送到界面：查询次数、已等待秒数、HTTP 状态。
- 点击后立即创建 `image-debug/<镜头号>-poll.json`，轮询期间持续更新。
- 每次任务查询增加 20 秒单请求超时，避免 fetch 永久挂起。
- 成功后立即重新读取生成图片并刷新分镜预览。
- 任务详情增加“查看图片任务日志”按钮。
- 重做成功后主进程发送明确完成事件。

## 正常表现

点击第 9 镜“重做画面”后，界面会依次显示类似：

- 第 9 镜正在等待图片结果：第 1 次查询，已等待 0 秒
- 第 9 镜正在等待图片结果：第 2 次查询，已等待 5 秒，接口返回 502
- 第 9 镜图片已生成……

任务目录中会立刻出现：

```text
image-debug/9-poll.json
```

成功后镜头预览会自动刷新，不需要关闭再打开任务。

## 说明

如果 `dm-fox.rjj.cc/api/image-tasks` 持续返回 502，程序最终仍会报超时。这代表中转站的任务查询网关不可用，本地程序无法把一个持续 502 的接口变成有效结果；0.8.3 主要解决等待过程不可见、请求可能永久挂起以及成功后预览不立即刷新的问题。
