# Storybound 0.8.2：图片任务查询地址误当图片地址修复

## 根因

接口首次响应中的：

```json
{
  "response": {
    "poll_url": "https://dm-fox.rjj.cc/api/image-tasks?ids=sync-gen-..."
  }
}
```

`poll_url` 是异步图片任务的状态/结果查询地址，不是图片文件地址。

0.8.0/0.8.1 的递归图片字段兼容逻辑把任何 HTTP URL 都当成图片地址，因此将 `response.poll_url` 直接交给图片下载函数。查询接口当时返回 nginx 502，程序便误报为“下载图片结果失败”。

## 0.8.2 修改

1. `poll_url`、`status_url`、`task_url`、`query_url` 等字段不再被识别为图片地址。
2. 自动识别响应中的任务查询地址，无须手动配置异步模式。
3. 对查询地址执行长期轮询，而不是按图片下载只重试 6 次。
4. 轮询期间的 502、503、504、429、404 被视作任务暂未就绪并继续等待。
5. 查询响应可直接返回图片二进制、Base64、图片 URL 或嵌套 JSON，程序都会继续提取。
6. 轮询最长默认约 360 秒，失败时生成 `image-debug/<镜头>-poll.json`。
7. 识别到 `poll_url` 后立刻写入分镜断点，程序退出后可继续查询，不重新提交生成任务。
8. 单镜“重做画面”会自动读取旧的 `<镜头>-download.json`，恢复已经存在的 `poll_url`，优先取回旧任务结果，避免再次计费。
9. 文生图与参考图编辑都支持 `poll_url`。

## 本地测试

已通过以下场景：

- 提交接口返回 `response.poll_url`；
- 查询接口前两次返回 nginx 502；
- 随后返回 `in_progress`；
- 最终返回嵌套 Base64 图片；
- 中断后使用保存的查询地址恢复；
- 从 0.8.1 生成的 `<镜头>-download.json` 自动恢复旧任务，不再次 POST 提交。

## 用户操作

覆盖 0.8.2 后直接打开原任务，对第 9 镜点击一次“重做画面”。程序会优先读取：

```text
image-debug\9-download.json
```

中的旧 `poll_url`，查询已有任务，不会先重新提交图片生成。

如果查询地址持续约 6 分钟都返回 502，则属于中转站查询服务不可用。新的调试文件为：

```text
image-debug\9-poll.json
```
