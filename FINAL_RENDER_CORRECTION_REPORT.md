# Storybound 0.8.6 最终渲染修正

## 截图中问题的根因

1. 0.8.5 将主标题、副标题、旁白字幕和免责声明同时烧录到整个视频。
2. 主标题使用 `script.title`，副标题错误地使用 `script.summary`，两者原先从 0 秒持续到视频结束。
3. 默认模板中的副标题和旁白字幕 Y 坐标几乎相同，导致白色摘要与黄色字幕互相覆盖。
4. 每个镜头整段旁白一次性显示，不是真正的逐句字幕。
5. 左拉镜使用 2 像素整数步进裁剪，移动速度较慢时会出现“停一下、跳一下”的抖动感。

## 0.8.6 调整

- 主标题与副标题仅作为片头文字显示，默认 3.2 秒，可在重新合成窗口调节为 0–8 秒。
- 旁白按照中文标点拆分，每次最多两行，并按文字长度分配到镜头音频时长。
- 自动文字避让：片头标题置于上部，逐句字幕置于下部，免责声明置于底部。
- 主标题最多 24 个字；副标题只取摘要首句并限制 32 个字，避免占满画面。
- 修正 ASS 字体缩放，不再把模板字号放大到过大的程度。
- 左右拉镜改为二倍超采样：在高分辨率画面上逐帧移动，再缩回输出尺寸，减少整数像素跳动。
- 默认运镜强度由 1.0 调低为 0.75。
- 重新合成窗口中的选项改名，明确区分“片头副标题”和“逐句旁白字幕”。

## 对标程序的合理结构

静态解包可以确认目标程序将封面标题、草稿模板文字层、逐镜旁白字幕和画面素材作为不同对象交给剪映草稿组件处理。它们不应被合并成一块全程显示的文字：

- 视频标题/摘要用于封面或片头；
- 旁白字幕按镜头或句子跟随时间轴；
- 安全提示是独立的小字图层；
- 动画属于图片/视频轨道，而不是文字图层。

由于只做了静态分析，目标程序所有模板的精确显示秒数无法从安装包中完全确认。0.8.6 采用片头 3.2 秒的通用实现，并提供可调节选项。

## 使用

更新后打开已完成任务，选择“使用现有素材重新合成”：

- 动画：左拉镜
- 运镜强度：0.50–0.75
- 使用静态图片：开启
- 逐句旁白字幕：开启
- 片头主标题：按需开启
- 片头副标题：按需开启
- 自动避让文字：开启
- 片头标题时长：建议 2.5–3.2 秒

生成文件仍为 `final-rerender.mp4`，不会重新调用图片、文案或配音接口。
