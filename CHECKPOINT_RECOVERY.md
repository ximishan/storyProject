# 0.7.4 断点续跑与停电恢复说明

本版本把原来“失败后手动重跑”的任务流程改成了按阶段、按分镜持久化的恢复机制。目标是：断网、接口超时、强制关闭软件或电脑突然关机后，重新打开软件可以复用已完成结果，从未完成的位置继续。

## 一、恢复范围

任务会按以下阶段保存检查点：

1. 文案改写
2. 主角/产品档案提取
3. 分镜规划和生图提示词
4. 逐镜图片生成
5. 逐镜配音
6. 逐镜动态视频
7. 逐镜渲染片段
8. 最终视频合成
9. 封面生成
10. 剪映草稿生成

每个阶段完成后都会立即写入任务目录中的 `pipeline.json`，同时更新 SQLite 任务记录。不是等整条视频完成后才保存。

## 二、软件重启后的行为

软件启动时会检查数据库里上次仍为 `running` 的任务，并自动改为：

```text
interrupted（已中断）
```

任务卡片和详情页会显示“从断点继续”。点击后会：

- 读取原任务的 `output_dir`
- 读取并校验 `pipeline.json`
- 复用已完成的大模型阶段
- 复用已成功保存的图片、音频、动态视频和分镜片段
- 查询仍有远程任务 ID 的异步生成任务
- 只处理未完成或文件损坏的分镜

## 三、大模型阶段缓存

任务目录的 `llm-debug` 中会保存：

```text
01-rewrite-checkpoint.json
02-metadata-checkpoint.json
03-scenes-checkpoint.json
```

每个缓存都带输入指纹。只有以下内容未变化时才会复用：

- 原始文案
- 处理模式和目标参数
- 提示词模板
- 使用的模型和接口身份
- 上一步输出

用户修改文案、模板或相关配置后，对应缓存会自动失效，不会误用旧结果。

## 四、逐镜状态

每个分镜分别保存：

```text
image_status / image_attempts / image_error
image_remote_task_id / image_remote_provider
audio_status / audio_attempts / audio_error
video_status / video_attempts / video_error
video_remote_task_id / video_remote_model
render_clip_status
```

例如第 1～5 张图片已经成功，第 6 张生成时停电：重新启动后只会检查第 6 张及后续分镜，第 1～5 张不会重新请求。

## 五、异步接口任务恢复

RunningHub 和支持异步任务 ID 的自定义图片接口会在提交成功后立即保存：

```text
task_id / request_id / job_id
```

重新启动时优先查询旧任务，不直接重复提交。这样可以减少重复扣费和重复生成。

对于同步 OpenAI 兼容接口，程序会发送稳定的幂等请求标识，但最终是否防止重复计费取决于接口服务商是否支持幂等语义。如果服务端已经生成成功、但电脑在收到响应前断电，仍存在重复请求的可能。

## 六、文件安全

重要 JSON 文件采用原子写入：

```text
先写临时文件
→ 写入完成并关闭
→ 替换正式文件
```

可以降低写入一半时突然断电导致 `pipeline.json` 损坏的概率。

恢复时还会检查文件是否真实存在且大小有效，不能仅依靠数据库中的“completed”状态。

## 七、FFmpeg 恢复边界

FFmpeg 不能从最终 MP4 的某一帧继续编码，但本版本会复用已经完成的逐镜片段：

```text
rendered/scene-001.mp4
rendered/scene-002.mp4
...
```

因此停电后通常只需补齐未完成片段，再重新执行最后一次拼接，不必重新处理所有图片、配音和分镜片段。

## 八、持久化队列

批量队列现在会把 `queue_order` 和 `queue_batch_id` 保存到数据库。软件重新启动后，如果还有排队任务，任务页会显示“恢复上次队列”。

正在执行的任务会显示“已中断”，尚未开始的任务继续保持“排队中”。

## 九、1 分钟测试建议

第一次测试建议控制在 4～8 个分镜，并先关闭动态分镜，减少变量：

1. 新建约 1 分钟的旁白任务。
2. 选择 4～8 个分镜。
3. 动态分镜选择“关闭”。
4. 开始生成。
5. 等待至少完成 1～2 张图片或音频。
6. 直接关闭软件，或者在测试环境中重启电脑。
7. 再次打开软件。
8. 确认任务状态变为“已中断”。
9. 点击“从断点继续”。
10. 检查之前完成的图片、音频修改时间是否未变化，仅缺失部分继续生成。

确认静态流程恢复正常后，再开启动态分镜测试异步任务 ID 的恢复。

## 十、验证命令

```powershell
npm run build
npm run test:llm-planner
npm run test:reference-routing
npm run test:openai-image
npm run test:dynamic
npm run test:checkpoint
```

`test:checkpoint` 会模拟：生成两张图片后中断、删除第二张并标记任务中断、再次运行，然后验证第一张保持不变、第二张自动补齐。

## 十一、当前环境限制

- 未使用用户的真实 API Key，因此真实第三方接口的断线恢复仍需在用户电脑上验证。
- 当前 Linux 沙箱无法使用 Windows SAPI，因此完整 Windows 配音烟雾测试不能在这里完成。
- 当前沙箱无法联网下载 Node 原生头文件，`better-sqlite3` 的 Linux 本地重编译未完成；数据库迁移代码已通过静态检查，Windows 安装依赖时会按 Windows 环境安装原生模块。
