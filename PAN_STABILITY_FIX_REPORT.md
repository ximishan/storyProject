# Storybound 0.8.7 运镜稳定性修复

## 现象

0.8.6 的字幕已经正常，但“左拉镜 / 右拉镜”仍然有明显的顿挫和抖动感。

## 根因

0.8.6 虽然使用了二倍超采样，但仍然存在两个问题：

1. 在 30fps 下从超扫区域的一端移动到另一端，短镜头每帧位移过大。
2. 裁剪坐标最终仍需落到整数像素，容易形成“停一下、跳一下”的步进感。

以 1080×1920、1 秒镜头为例，旧实现约每帧移动 5 像素，主体轮廓会明显跳动。

## 0.8.7 调整

- 左拉镜、右拉镜、交替拉镜统一改为 60fps 渲染。
- 不再横跨整个超扫区域，只在画面中心附近做小幅平移。
- 位移范围限制为画面宽度约 3.5%～7%，短镜头不会快速扫动。
- 继续在二倍画布上裁剪后缩回目标尺寸。
- 增加相邻帧轻量混合，弱化整数像素步进。
- 为每个视频片段重建稳定时间基，避免重复帧和不均匀时间戳。
- 默认运镜强度改为 0.50。
- “无动画”仍保留，可用于完全静止画面。

## 本地验证

使用同一张 1080×1920 测试图生成 1 秒视频：

- 0.8.6：30fps，约 162 像素总位移，约 5.4 像素/帧。
- 0.8.7：60fps，约 46 像素总位移，约 0.8 像素/帧，并带轻量帧混合。

新版的单帧位移明显降低，适合人物纪实类竖屏视频。

## 使用

打开已经完成的任务，选择“使用现有素材重新合成”：

- 动画：左拉镜
- 运镜强度：0.50
- 全部使用现有静态图片：开启
- 逐句旁白字幕：开启

生成文件：

`final-rerender.mp4`

不重新调用文案、图片和配音接口。
