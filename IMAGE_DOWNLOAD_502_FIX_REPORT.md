# Storybound 0.8.1：图片地址下载 502 修复

## 问题表现

图片生成接口已成功返回图片地址，但程序随后下载该地址时收到：

```text
下载图片结果失败 (502)
```

这与提示词审核无关。它说明模型生成阶段已经完成，失败发生在返回图片文件的下载阶段。

## 常见原因

1. 中转站返回的临时图片地址尚未完全就绪。
2. 图片 CDN 或网关短暂返回 502/503/504。
3. 图片地址与 API 同源，并且下载时也要求 Bearer 鉴权。
4. 返回的所谓图片地址实际还会返回一层 JSON，真实图片地址在该 JSON 内。
5. 地址中包含 `&amp;`、`\u0026` 或转义斜杠，导致签名参数失真。

## 0.8.1 修改

- 图片地址遇到 408、409、425、429、500、502、503、504 时自动重试，最多 6 次。
- 支持 `Retry-After` 响应头和指数退避，避免立即重复打爆网关。
- 下载地址与 API 同源时自动附带 Bearer API Key；跨域 CDN 不发送 Key，避免密钥泄露。
- 下载时增加图片 Accept、浏览器 User-Agent、Referer 等兼容请求头。
- 自动修复 `&amp;`、`\u0026`、转义斜杠形式的图片 URL。
- 下载地址返回 JSON 时，再递归提取真实图片 URL 或 Base64。
- 最终失败会保存：

```text
任务产物目录/image-debug/镜头编号-download.json
```

文件记录每次下载的状态码、内容类型和脱敏后的错误正文，不保存 API Key。

## 使用方法

1. 停止旧程序。
2. 覆盖为 0.8.1 源码。
3. 执行 `npm run dev`。
4. 打开原任务，只点击失败镜头的“重做画面”。
5. 不需要重新生成文案和分镜，也不需要继续修改当前安全提示词。

如果 6 次重试后仍然失败，请提供对应的 `镜头编号-download.json`，即可确认是临时 CDN 故障、鉴权要求还是中转站返回了不可访问地址。

## 测试

已通过：

- 临时 502 两次后第三次成功下载。
- 同源图片地址要求 Bearer 鉴权。
- 图片下载地址返回二次 JSON 跳转。
- Base64、嵌套 URL、Markdown 图片链接。
- 图片提示词安全改写。
- LLM JSON 修复及协议兼容测试。
