# Storybound 0.7.8 图片内容审核修复说明

## 本次错误

错误发生在图片生成阶段：

```text
The request was rejected by the content policy.
services.cjs -> generateOpenAiImage
pipeline.cjs -> imageWorker
```

这表示前面的文案改写、JSON 解析和分镜生成已经成功，失败的是某一镜的图片提示词被图片服务审核拒绝。历史人物、战地、手术、伤口、死亡等词语组合，尤其带有写实摄影、特写、真实人物面容等要求时，容易触发图片接口审核。

## 0.7.8 修改

1. 识别图片接口的内容审核错误，不再把它当成普通网络错误。
2. 首次审核拒绝后，自动把提示词改成克制、非细节化的历史纪实画面再请求。
3. 第二次仍被拒绝时，再生成更严格的安全版本，删除手术、伤口、血液、尸体、暴力特写等片段。
4. 文生图和参考图编辑都支持审核改写重试。
5. 任务记录新增：
   - `image_prompt_used`
   - `image_policy_adjusted`
6. 报错会明确显示“第几镜图片生成失败”，不再只显示接口原始错误。
7. 任务目录新增 `image-debug`，保存原始提示词、改写提示词和接口错误，不包含 API Key。
8. 图片接口设置新增：
   - 审核灵敏度：不传 / auto / low
   - 审核拒绝后自动改写重试开关

## 建议设置

中转站兼容性不明确时，审核灵敏度保持“不传”。如果中转站明确支持 GPT Image 的 `moderation` 参数，可选择 `low`。`low` 只是较宽松的官方审核模式，不会关闭内容安全限制。

## 断点续跑

本次任务已经完成分镜，只在图片阶段失败。覆盖新版后直接重新运行原任务，程序会复用已有文案和分镜，只补生成失败或缺失的画面。

## 测试

已通过：

- OpenAI 兼容图片生成和参考图编辑测试
- 内容审核拒绝后自动改写测试
- LLM 分阶段规划测试
- LLM 协议兼容测试
- LLM JSON 修复测试
- 单镜断点恢复测试
- 动态分镜测试
- 脚本模式测试
