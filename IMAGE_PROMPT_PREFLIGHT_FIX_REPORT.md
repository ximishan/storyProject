# Storybound 0.7.9：图片提示词生成前安全改写修复

## 问题原因

0.7.8 只在图片接口已经返回内容审核拒绝后才改写提示词，而且界面中的“最终图片提示词”仍显示原始分镜提示词。

旧的关键词替换还存在语义残留。例如删除“血液”后，提示词仍可能保留：

- 新鲜割口清晰可见
- 暗红色沿指缝渗出滴落
- 极近景、微距特写
- 染血纱布
- 伤口缝合操作

因此即使程序显示“已自动改写”，提交给接口的第二、第三版提示词仍可能被审核拒绝。

## 0.7.9 修改

1. 新增 `electron/image-prompt-safety.cjs`，统一负责图片提示词风险识别和改写。
2. 分镜模型提示词增加硬性约束，要求医疗、受伤、暴力和死亡内容使用克制替代画面。
3. 大模型返回分镜后，立即检查并改写 `desc_prompt` 和最终 `image_prompt`。
4. 图片接口调用前再次检查，确保样式前后缀不会重新带入高风险细节。
5. 首次请求直接发送安全提示词，不再先发送明显会被拒绝的原始提示词。
6. 如仍被拒绝，依次降级为“最小医疗叙事画面”和“纯环境人物画面”。
7. “重做画面”按钮走同一套安全检查逻辑。
8. 打开旧任务时会自动迁移已保存的高风险提示词，并更新 `pipeline.json`、`script.json` 和数据库记录。
9. 界面会显示“已自动替换高风险画面”的提示，最终图片提示词会显示实际准备发送的安全版本。

## 镜头 09 的改写示例

原提示词包含直接的伤口、流体和缝合细节。0.7.9 会在请求前改为类似：

> 1939年10月，中国华北前线，黑白纪实摄影，一位外国医生的演员化历史形象站在简朴医疗站内，低头查看已经用干净纱布妥善包扎好的左手食指，身后医护人员整理医疗器械，患者区域由布帘和景深自然遮挡，人物神情专注而疲惫，煤油灯侧光，中近景叙事镜头，真实胶片颗粒，情绪克制，适合大众观看，9:16竖构图，主体明确，构图完整，无文字无水印。

旁白仍保留原始事实，但图片不再直观展示令人不适的细节。

## 测试结果

通过以下本地测试：

- 图片提示词主动安全改写测试
- OpenAI 兼容图片生成与参考图编辑模拟测试
- 三阶段 LLM 规划与检查点复用测试
- LLM JSON 修复测试
- LLM 协议兼容测试
- Electron CJS 文件语法检查
- TypeScript/TSX 语法转译检查

模拟测试确认高风险原始提示词不会发送到图片接口。真实中转接口仍需在用户电脑上验证。

## 使用方式

覆盖旧源码后执行：

```powershell
cd D:\storybound\source
npm run dev
```

打开原失败任务时，旧提示词会自动迁移。随后只需要点击镜头 09 的“重做画面”，不需要重新生成全部文案和分镜。
