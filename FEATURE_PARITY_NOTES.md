
## 0.9.1 配音试听差异修正

原版的“本地内置试听”并不是随安装包附带 185 个固定 MP3，而是完整内置音色目录，并在第一次调用火山 TTS 后把试听结果保存到本机。当前版本已补齐：

- 185 个音色、6 个分类、1.0/2.0 版本切换；
- 搜索、最多 5 个收藏、开始创作页和 TTS 设置页共用同一音色选择器；
- 固定试听文案“他来到江南一个小村庄”；
- 按音色、语速和试听文本生成 SHA-256 缓存键；
- 缓存保存到应用数据目录 `voice-preview-cache/v2`，重启后仍可直接播放；
- 未配置 App ID / Access Token 时允许选择音色，但明确禁止首次在线试听；
- Windows 系统音色继续支持完全离线试听。

# Storybound 0.9.0 功能差异核对与合并说明

## 修改基线

本次只以用户本轮上传的 `source(4).zip` 为基线。原压缩包保持不变，所有修改均在复制目录中完成。

- `source(4).zip` SHA-256：`85de918cffe07ef8b03fb51e65fdf3868d46e66d7921db87584f6779df32eb09`
- 对照安装包 SHA-256：`f3c043a603413ee850f09ca530871a2659ae388e54d848557df56596372e2e59`

## 本次已修正的差异

### 1. 创建页画面风格悬浮预览

位置严格放在：

`开始创作 → 出图 → 画面风格`

12 个内置风格按钮均支持鼠标悬浮预览。预览图使用本轮提供的 Storybound 1.7.0 安装包内对应 WEBP 资源，不再使用重新生成的替代图。

### 2. 产品参考图与人物参考图分离

- 人物类赛道：显示人物一致性、上传主角参考图、系统九宫格人物卡。
- 产品类赛道：显示“产品参考图 · 可选”。
- 新任务分别保存 `reference_image_path` 和 `product_reference_image_path`。
- 每个镜头根据 `subject_presence` 选择人物图、产品图或两者。
- 产品类旧任务若过去把产品图保存在 `reference_image_path`，仍会自动兼容。

### 3. 参考图发送规则

- `subject_presence=character`：只发送人物参考图。
- `subject_presence=product`：只发送产品参考图。
- `subject_presence=both`：支持多图的接口会同时发送人物与产品图；只支持单图的 RunningHub 接口使用第一张有效参考图。
- `use_reference=false`：不发送参考图，继续走普通文生图。

### 4. 模板比例联动

选择草稿模板后，AI 出图比例按钮进入锁定状态，避免出现“已跟随草稿模板”但仍可手动误改的问题。取消或改用允许自定义比例的模板流程后再调整比例。

### 5. 其他界面及协议修正

- “图片实验室”统一为“画图实验室”。
- 版本号从 `package.json` 读取，不再固定显示旧版 `v0.8.3`。
- 参考图编辑提示同时适配人物、产品、书籍、菜品与关键物件。
- 自定义提示词模板选择“跟随赛道默认”时，会正确回退到当前赛道的参考图类型。

## 已核对且当前源码已有的能力

- 粘贴文案与 AI 研究创作。
- 故事旁白与双人播客。
- 全自动、半自动、直接出片。
- 目标字数、目标分镜、改写强度、叙事视角及推广内容处理。
- AI 图片与网络素材。
- 草稿模板、独立 3:4 封面、配音、语速和背景音乐。
- 暂停确认节点。
- 动态分镜关闭/前几张/全部/自定义数量。
- 动态视频跟随配音或固定时长，失败回退静态图。
- 双人播客半自动/直接模式的 `[A]`、`[B]` 逐行校验。
- RunningHub 官方图片模型、图生视频主模型与兜底模型。
- 字幕末尾普通标点清理及语义分段。
- 剪映风格图片动画与现有素材重新合成。

## 保留的合理差异

以下内容没有复制或伪造：

- 原程序的账号、试用、激活、许可证和硬件绑定模块。
- 原程序的更新服务器、私有接口地址、密钥保存方式和服务端批次能力。
- 原版 `storybound.exe`、`draft-generator.exe` 等第三方二进制。
- IMA 私有数据源。
- Tauri/Rust 架构；当前项目继续使用 Electron + React + TypeScript。

这些内容不影响本地 AI 视频创作主流程。剪映草稿仍需用户自行放置有权使用的 `resources/draft-generator.exe`。

## 验证命令

```powershell
npm run test:style-previews
npm run test:references
npm run test:parity
npm run test:voice-preview-parity
npm run test:caption
npm run test:prompts
npm run test:script-modes
npm run build
```

当前生成环境无法从内部 npm 镜像下载 `@xmldom/xmldom@0.8.23`，因此没有在此环境完成全量 `npm ci` 和 Vite 生产构建。已完成无需下载依赖的 CJS 语法检查、TypeScript/TSX 转译检查以及上述本地逻辑测试。
